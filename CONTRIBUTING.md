# Contributing to DungeonDirectory

Thanks for helping! Quick start:

1. `git clone <repo>`
2. `git switch ui/chat-threads`
3. `docker compose up -d`
4. Make changes; commit with *Conventional Commits* style (`git cz`).
5. `git push`

CI runs lint, tests, security audit.
