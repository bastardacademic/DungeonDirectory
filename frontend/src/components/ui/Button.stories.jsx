import Button from "./Button";

export default { title: "UI/Button", component: Button };

export const Primary = { args: { children: "Primary Button" } };
export const Ghost   = { args: { children: "Ghost Button", variant: "ghost" } };
